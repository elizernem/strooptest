// const isFilledIn = (input, submit) => {
//   if (input.required == true && input.value === "") {
//     submit.disabled = true;
//   } else {
//     submit.disabled = false;
//   }
// };

// const validateInput = (time, count, submit) => {
//   if (
//     (time.required == true &&
//       (time.value > 5 || time.value < 0.5 || time.value === "")) ||
//     (count.required == true &&
//       (count.value < 1 || count.value > 100 || count.value === ""))
//   ) {
//     submit.disabled == true;
//   } else {
//     submit.disabled = false;
//   }
// };

// const validateTime = (time, submit) => {
//   if (
//     time.required == true &&
//     (time.value > 5 || time.value < 0.5 || time.value === "")
//   ) {
//     submit.disabled == true;
//   } else {
//     submit.disabled = false;
//   }
// };

// const validateCount = (count, submit) => {
//   if (
//     count.required == true &&
//     (count.value < 1 || count.value > 100 || count.value === "")
//   ) {
//     submit.disabled == true;
//   } else {
//     submit.disabled = false;
//   }
// };

// export { validateTime, validateCount };
